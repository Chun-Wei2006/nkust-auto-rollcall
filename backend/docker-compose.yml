services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    networks:
      - tunnel-network

networks:
  tunnel-network:
    name: tunnel-network
    external: true
